import Request from 'src/utils/request'

export function uploadFileApi (data) {
  return Request({
    url: 'xxx',
    method: 'post',
    data
  })
}

export function postUrlListApi (data) {
  return Request({
    url: 'xxx',
    method: 'post',
    data
  })
}
